#include <stdio.h>
#include <stdlib.h>

int main()
{
    int sum,a,b;
    printf("enter the values of a and b\n");
    scanf("%d%d",&a,&b);
    if(a <= b)
    {
        printf("yes");

    }
    else
    {
        printf("false");
    }
    return 0;
}
